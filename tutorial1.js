var firstName='Sid';// Old way to declare variable
let lastName='Kadam'; //New way to declare variable and it uses camelCase 
let age;
age=23;
console.log(firstName,lastName);// Used to display the output in javascript
console.log(age);

const pi=3.14;// Constant value cannot be changed
console.log(pi);

const COLOR_MOON='White';
console.log(COLOR_MOON); //Used Snake Cased(SNAKE_CASE) for declaring constant values


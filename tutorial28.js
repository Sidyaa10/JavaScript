//Insight in Garbage collection by deallocating datatypes that are not being used anymore and frees up memory space.
//JavaScript has a built in math function it helps us do mathematical calculations refer to the math functions on developer.mozilla.org which has all of them listed.

let min=1;
let max=25;

const randomNum =Math.random()*(max-min) +min;
console.log(randomNum);